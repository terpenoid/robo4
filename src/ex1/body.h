struct coord {
  int x;
  int y;
  int z;
};

class Body {

    int bodyWidth = 70;
    int bodyHeight = 70;

//    Leg legs[4];

  public:
    Body() {
//      legs[0] = leg1;
//      legs[1] = leg2;
//      legs[2] = leg3;
//      legs[3] = leg4;
    }

//    Leg getLeg(String leg) {
//      if (leg == "fl") return legs[0];
//      if (leg == "fr") return legs[1];
//      if (leg == "br") return legs[2];
//      if (leg == "bl") return legs[3];
//    }
//
//    coord getLegPos(String leg) {
//      return { x: getLeg(leg).getX(), y: getLeg(leg).getY(), z: getLeg(leg).getZ() };
//    }

    

};
